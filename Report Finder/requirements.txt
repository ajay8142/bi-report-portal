# Runtime deps for search_api.py only (build_report_vectorstore.py / test.py
# are dev-time CLI tools, not run inside the container — see .dockerignore).
#
# chromadb's on-disk collection-config schema is NOT stable across major
# versions — chroma_store/chroma.sqlite3 was built with the versions pinned
# below (matches whatever built build_report_vectorstore.py's output). A
# newer/older chromadb here will fail to read it (KeyError: '_type' on
# startup). If you rebuild chroma_store with different package versions,
# update these pins to match.
#
# CPU-only torch wheel keeps the image from pulling in CUDA (~4GB smaller).
--extra-index-url https://download.pytorch.org/whl/cpu

flask==3.1.3
flask-cors==6.0.5
chromadb==1.5.9
langchain-huggingface==1.2.2
sentence-transformers==5.6.0
torch==2.13.0
